## Version 1.8.1

### Fixed
- Fixed a missing-mod screen not appearing when CRNet is not installed (the mod now properly declares CRNet as a required dependency, so players see a clear error instead of a cryptic crash).
